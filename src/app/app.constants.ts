export const ionColorPrimary = getComputedStyle(document.body).getPropertyValue("--ion-color-primary").trim();
export const ionColorSecondary = getComputedStyle(document.body).getPropertyValue("--ion-color-secondary").trim();
